spring:
  application:
    name: 'service-discovery'
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: ${PORT:8761}